function Shutdown() {
    return (
        <div className="description">
            <p>On shutdown, visualisation will terminate for this session. Analysis will continue as usual. 
                If you are certain you want to shutdown the server, refresh the page.</p>
        </div>
    )
}

export default Shutdown;